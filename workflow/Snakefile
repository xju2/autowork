from snakemake.utils import min_version
min_version("9.0")

# configfile path is interpreted relative to the working directory
# not relative to the location of the snakefile that contains the statement.
configfile: "config/config.yaml"
report: "report/workflow.rst"


include: "rules/athena_dev.smk"
include: "rules/traccc.smk"
include: "rules/tracking.smk"
include: "rules/triton.smk"
include: "rules/test.smk"

SAMPLES = ["MuonPU0", "HaaPU0", "HaaPU200"]
rule all:
    input:
        # expand("workarea/tracking/{sample}/idpvm.ckf.local.gnn4itkTriton.none.{sample}.root", sample=SAMPLES)
        expand("workarea/tracking/{sample}/idpvm.gnn4itkML.triton.gnn4itkTriton.tracking.{sample}.root", sample=SAMPLES)
        # "workarea/tracking/ttbar/aod.ckf.ttbar.root"
